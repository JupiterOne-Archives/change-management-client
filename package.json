{
  "name": "@jupiterone/jupiter-change-management-client",
  "version": "0.1.0",
  "description": "A Node change management helper.",
  "main": "dist/changeManagementClient.js",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "yarn lint && rm -rf dist && tsc --project tsconfig.dist.json --declaration",
    "test": "jest",
    "lint": "eslint src test --ext .ts",
    "format": "prettier --write '**/*.{ts,js,json,md}'"
  },
  "dependencies": {
    "@jupiterone/jupiterone-client-nodejs": "0.15.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "@types/node": "~8.10.0",
    "@types/uuid": "^3.4.5",
    "@typescript-eslint/eslint-plugin": "^2.2.0",
    "@typescript-eslint/parser": "^2.2.0",
    "eslint": "^6.3.0",
    "eslint-config-prettier": "^6.3.0",
    "husky": "^3.0.5",
    "jest": "^24.9.0",
    "lint-staged": "8.2.1",
    "prettier": "^1.18.2",
    "ts-jest": "^24.0.2",
    "typescript": "^3.6.3"
  }
}
